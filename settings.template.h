// Точка доступа
const char* SSID = "";              // Название        
const char* SSID_PASSWORD = "";     // Пароль

// SMTP-сервер
const char* SMTP_SERVER_ADDRESS = "";       // Адрес (например, "smtp.mail.ru")
const int SMTP_SERVER_PORT = 0;             // Номер порта (465)
const String SMTP_SERVER_LOGIN = "";        // Логин ("some-mail@mail.ru")
const char* SMTP_SERVER_PASSWORD = "";      // Пароль

// Адреса получателей писем
const String MAIL_TO_FIRST = "";
const String MAIL_TO_SECOND = "";